% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_analysisDS.R
\name{period_group_statsDS}
\alias{period_group_statsDS}
\title{Per-period (before/after visit) summary of one column}
\usage{
period_group_statsDS(df, value_col, visit_col, visit_cutoff, nfilter = 5)
}
\arguments{
\item{df}{A data frame.}

\item{value_col}{Character, the column to summarize (label or feature).}

\item{visit_col}{Character, the visit/time column.}

\item{visit_cutoff}{Numeric cutoff; \code{visit_col < visit_cutoff} is
"before", \code{>=} is "after".}

\item{nfilter}{Minimum per-period complete-case n required.}
}
\value{
A named list with entries \code{"before"}/\code{"after"} (only
  for periods with n >= nfilter), each with \code{n, mean, sd, min, q25,
  median, q75, max}.
}
\description{
Derives a before/after period from \code{visit_col}/
  \code{visit_cutoff} (see \code{cdh_period_factor}) and reports, for
  each period, the same n/five-number-summary as
  \code{group_summary_statsDS} -- used for both the label's and the
  feature's own before/after distribution (boxplots 08/09), and to get
  \code{label_mean/median}, \code{feature_mean/median}-style numbers for
  the longitudinal comparison table (section 15).
}
