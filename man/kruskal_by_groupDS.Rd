% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_analysisDS.R
\name{kruskal_by_groupDS}
\alias{kruskal_by_groupDS}
\title{Kruskal-Wallis test of several features across levels of a group column}
\usage{
kruskal_by_groupDS(df, features, group_col, nfilter = 5)
}
\arguments{
\item{df}{A data frame.}

\item{features}{Character vector (or "$"-joined string) of feature column names.}

\item{group_col}{Character, the grouping column (typically the label).}

\item{nfilter}{Minimum per-group complete-case n required.}
}
\value{
A list, one entry per feature, with \code{statistic}, \code{df},
  \code{p_value}, \code{n_total}, \code{group_ns} (named list of
  per-group n -- counts only).
}
\description{
Vectorized complete-case Kruskal-Wallis test, one call for
  every feature. A feature is skipped (test not run, \code{NA} returned)
  if any group has fewer than \code{nfilter} complete-case observations
  for that feature, or if fewer than 2 groups remain.
}
