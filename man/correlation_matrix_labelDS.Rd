% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/label_analysisDS.R
\name{correlation_matrix_labelDS}
\alias{correlation_matrix_labelDS}
\title{Spearman correlation matrix over a label and its clinical features}
\usage{
correlation_matrix_labelDS(df, label, features, nfilter = 5)
}
\arguments{
\item{df}{A data frame.}

\item{label}{Character, the label column (included first in the matrix).}

\item{features}{Character vector (or "$"-joined string) of feature column names.}

\item{nfilter}{Minimum pairwise-complete count required to release a cell.}
}
\value{
\code{list(cols=, matrix=)}, or \code{NULL} if fewer than 2 usable
  numeric columns.
}
\description{
Thin wrapper around the shared \code{cdh_correlation_matrix}
  helper (also used by \code{exploratory_analysisDS}), restricted to
  \code{label} plus \code{features} rather than every numeric column, so
  the resulting matrix stays focused and readable.
}
